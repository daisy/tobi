<DockPanel
    x:Class="Tobi.Modules.MetadataPane.MetadataPaneView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Tobi.Modules.MetadataPane"
    xmlns:UI="clr-namespace:Tobi.Common.UI;assembly=Tobi.Common"
    xmlns:toolkit="http://schemas.microsoft.com/wpf/2008/toolkit"
    LastChildFill="True" Loaded="DockPanel_Loaded"
    KeyUp="DockPanel_KeyUp">


    <DockPanel.Resources>

        <Style TargetType="{x:Type Button}">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style TargetType="{x:Type Button}" x:Key="DeleteButtonStyle">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Height" Value="10"/>
            <Setter Property="Background" Value="Red"/>
        </Style>
        <!--Basic Row Validation Error Template-->
        <ControlTemplate x:Key="BasicRowValidationErrorTemplate">
            <DockPanel LastChildFill="True">
                <Border BorderBrush="Red" BorderThickness="2">

                    <StackPanel Orientation="Vertical">
                        <AdornedElementPlaceholder Name="adornerPlaceholder"/>
                        <TextBlock Background="Red" Text="{Binding RelativeSource={RelativeSource
                                 FindAncestor, AncestorType={x:Type toolkit:DataGridRow}},
                                 Path=(Validation.Errors)[0].ErrorContent}"/>
                    </StackPanel>
                </Border>
            </DockPanel>
        </ControlTemplate>

        <!--Default Row Style-->
        <Style x:Key="defaultRowStyle" TargetType="{x:Type toolkit:DataGridRow}">
            <Setter Property="MaxHeight" Value="300" />
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource BasicRowValidationErrorTemplate}" />
            <Style.Triggers>
                <Trigger Property="IsEditing" Value="true">
                    <Setter Property="Background" Value="Pink"/>
                </Trigger>
                <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
    </DockPanel.Resources>

    <Grid DockPanel.Dock="Bottom">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Click="Add_Metadata_Button_Click" 
                Grid.Column="0" 
                Grid.Row="0">
                Add
            </Button>
        </StackPanel>
        <Button Click="Validate_Metadata_Click" 
                Grid.Column="2" 
                Grid.Row="0">
            Validate
        </Button>

        <!--<TextBox x:Name="LookupField" 
                 Grid.Row="1" 
                 Grid.Column="0" 
                 Grid.ColumnSpan="2" 
                 Height="20"/>
        <Button  Click="Lookup_Button_Click" 
                 Grid.Column="2" 
                 Grid.Row="1">
            Lookup
        </Button>
        -->

        <Button Click="Fake_Button_Click"
                Grid.Row="1"
                Grid.Column="0">
            Generate fake data</Button>
        <Button Click="Data_Model_Report_Click"
                Grid.Row="1"
                Grid.Column="1">
            View data model metadata</Button>
        
        <ListBox ItemsSource="{Binding ValidationErrors}"
                 Grid.Row="2" 
                 Grid.Column="0" 
                 Grid.ColumnSpan="3" MinHeight="100"/>
        
        
        
    </Grid>


    <!--<StackPanel Background="LightCoral">
            <TextBlock>Testing only:</TextBlock>
            
        </StackPanel>
    </StackPanel>-->

    <toolkit:DataGrid
        x:Name="MetadataGrid"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        ItemsSource="{Binding Metadatas, Mode=OneWay}"
        AutoGenerateColumns="False"
        SelectionMode="Single"
        RowStyle="{StaticResource defaultRowStyle}"
        RowDetailsVisibilityMode="VisibleWhenSelected"
        SelectionChanged="DataGrid_SelectionChanged"
        AlternationCount="2"
        ScrollViewer.CanContentScroll="True"
        ScrollViewer.VerticalScrollBarVisibility="Auto">
        
        
        <toolkit:DataGrid.RowValidationRules>
            <local:MetadataValidationRule/>
        </toolkit:DataGrid.RowValidationRules>
        
        <toolkit:DataGrid.RowDetailsTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding
                                    RelativeSource={RelativeSource FindAncestor,  
 	                                                AncestorType={x:Type local:MetadataPaneView}},  
 	                                Path=DataContext.SelectedMetadata.Definition.Description}"
                                    Background="LightYellow"/>
            </DataTemplate>
        </toolkit:DataGrid.RowDetailsTemplate>
        
        <toolkit:DataGrid.Columns>
            
            <toolkit:DataGridTemplateColumn Header="Name" Width="SizeToCells"
                                            IsReadOnly="False">
                <toolkit:DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"/>
                    </DataTemplate>
                </toolkit:DataGridTemplateColumn.CellTemplate>
                
                <toolkit:DataGridTemplateColumn.CellEditingTemplate>
                    <DataTemplate>
                        <ComboBox ItemsSource="{Binding
                                                    RelativeSource={RelativeSource FindAncestor,  
 	                                                AncestorType={x:Type local:MetadataPaneView}},  
 	                                                Path=DataContext.AvailableMetadata}"
                                  SelectedValue="{Binding 
                                                    RelativeSource={RelativeSource FindAncestor, 
                                                    AncestorType={x:Type local:MetadataPaneView}}, 
                                                    Path=DataContext.SelectedMetadata.Name}" 
                                   SelectionChanged="ComboBox_SelectionChanged"
                                  />
                    </DataTemplate>
                </toolkit:DataGridTemplateColumn.CellEditingTemplate>
            
            </toolkit:DataGridTemplateColumn>
            
            <toolkit:DataGridTemplateColumn Header="Content" Width="SizeToCells"  
                                               IsReadOnly="False">
                <toolkit:DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock x:Name="textBlock" Text="{Binding Content}"/>
                        </StackPanel>
                    </DataTemplate>
                </toolkit:DataGridTemplateColumn.CellTemplate>
                <toolkit:DataGridTemplateColumn.CellEditingTemplate>
                    <DataTemplate>
                            <TextBox x:Name="textBox" Text="{Binding Path=Content, 
                                        Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </DataTemplate>
                </toolkit:DataGridTemplateColumn.CellEditingTemplate>
            </toolkit:DataGridTemplateColumn>
            <toolkit:DataGridTemplateColumn>
                <toolkit:DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Button Content="X" Click="Remove_Metadata_Button_Click" Style="{StaticResource DeleteButtonStyle}"/>
                    </DataTemplate>
                </toolkit:DataGridTemplateColumn.CellTemplate>
            </toolkit:DataGridTemplateColumn>
        </toolkit:DataGrid.Columns>
        
    </toolkit:DataGrid>

</DockPanel>

